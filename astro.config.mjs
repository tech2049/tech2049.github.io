import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://tech2049.github.io', // آدرس سایت شما
  // گزینه base را کاملاً حذف کنید یا آن را به صورت زیر قرار دهید:
  base: '/', 
});